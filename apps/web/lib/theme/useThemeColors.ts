"use client";

// Re-exported from the provider module so chart components can import the hook
// from a stable path: `import { useThemeColors } from "@/lib/theme/useThemeColors"`.
export { useThemeColors } from "./ThemeProvider";
